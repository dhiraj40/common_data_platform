name: purchase_order

source:
  type: file
  format: csv
  path: purchase_order/
  header: true
  delimiter: ","

target:
  schema: raw
  table: purchase_order

load:
  mode: append

metadata:
  source_system: sap
